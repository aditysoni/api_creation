const EventEmitter = require("events") ; // creating a class with the help of events , we are passing a class in the place of a simple variable 

const event = new EventEmitter () ; // an object is created of name event




const fs = require('fs') ;

fs.readFile